package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class LaunchAlert {
    private WebDriver driver;

    public LaunchAlert(WebDriver driver){
        this.driver = driver;
    }

}
